# Design

Add parent-card metadata to agent-created sessions and render a presentation-only inline launcher. Promotion and return reuse existing tab activation; lookup is board-wide so moving either session between workspace columns does not break the launcher or return action. No provider, stream, persistence, or lifecycle semantics change.
