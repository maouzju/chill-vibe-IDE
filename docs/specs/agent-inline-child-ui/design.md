# Design

Add parent-card metadata to agent-created sessions and render a presentation-only inline launcher. Promotion and return reuse existing tab activation; no provider, stream, persistence, or lifecycle semantics change.
