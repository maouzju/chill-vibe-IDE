# Requirements

- Agent-created sessions remain the same real sessions; only presentation changes.
- New child sessions default to a compact inline launcher in the parent card.
- Clicking promotes the same session to its existing independent tab.
- The independent tab exposes a return control that restores inline presentation.
- Light/dark and narrow layouts remain readable.
