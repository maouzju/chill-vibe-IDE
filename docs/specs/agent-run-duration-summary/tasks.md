# Agent Run Duration Summary — Tasks

- [x] Define the persisted marker, formatting rules, lifecycle, and visual hierarchy.
- [x] Add failing focused tests for duration formatting and compact rendering.
- [x] Implement run timing lifecycle and terminal marker insertion.
- [x] Add quiet token-based styling and light/dark regression coverage.
- [x] Run targeted tests, quality checks, and visual verification. No repo-local Chill Vibe dev runtime was active; port 5173 belonged to another repository, so the packaged user instance was deliberately left untouched.
