# 默认超管模式

- 设置 → Agent 安全防护提供“默认开启超管模式”，默认关闭，保存后重启仍有效；旧存档按关闭处理。
- 开启后，用户新建普通会话（含新工作区首张卡）及未指定权限的看板需求默认具有工作区超管权限；已有会话不变，仍可单独开关。
- 看板已有参数、模板显式权限优先；工具卡不授予超管权限。
- 超管 MCP 自建会话仍强制无超管权限，自动循环、历史恢复及复制保持原有语义，避免授权扩散。
- 使用中英文文案及既有悬停说明，验证深浅主题。
