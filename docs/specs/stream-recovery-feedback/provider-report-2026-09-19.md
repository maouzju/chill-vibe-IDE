# 提交服务商：Codex 间歇性声称没有文件/命令工具

端点：`https://api.mouxihub.com/v1/responses`；请求模型 `gpt-6-astra`；CLI `0.153.4`。

## 现象

同一用户回合内，先正常执行 `custom_tool_call: exec`，后改为普通 JSON 代理通信工具，最终返回“没有文件/命令执行工具”，HTTP 200 且正常完成。9/19 凌晨主线程和子线程都在 00:49 后出现这种变化，无需重启或修改权限即可在后续请求恢复。

尚未证明实际底层模型、具体渠道或工具被丢弃；请不要仅凭响应 ID 前缀归因。

## 对照请求（北京时间 UTC+8）

| 时间 | 请求编号 | 观测 |
| --- | --- | --- |
| 9/19 00:49:35 | `MC202609181649316047867968268d9d6iohcl1lk` | 子线程正常 `custom_tool_call: exec` |
| 9/19 00:49:37 | `MC202609181649289520574148268d9d6msWHWC8e` | 主线程正常 `custom_tool_call: exec` |
| 9/19 00:50:21 | `MC202609181650134617078278268d9d6IhKsNBRL` | 子线程声称工具不可用 |
| 9/19 00:51:19 | `MC202609181651043972871518268d9d6Y9oFGjlM` | 主线程声称工具不可用 |

## 请核查

1. 对照请求实际命中的渠道、模型映射、回退路径是否发生变化。
2. 比较入站和出站工具定义：新版 CLI 正常请求将工具放在 `input[]` 的 `additional_tools` 项，使用 namespace；`functions.exec` 是 `custom`，代理通信工具是 `function`。请检查两种类型是否都完整传递。
3. 是否在 Responses 与其他接口间转换时遗漏 custom 工具、未转换 custom 工具返回值，或截掉工具说明；请保留原始 item 类型核对。
4. 更早 9/18 18:33 样本出现过 `cursor-sand-v1:` 及流事件缺失，不能先假定全部同源，但可一并查对应渠道。

## 新增：请优先查这一对协议异常 / 重试请求

- **9/19 00:46:29 北京时间**：`MC202609181646259516005488268d9d6EcfCZN7N`，HTTP 200 后 CLI 报 `OutputTextDelta without active item`；00:47:34 再报 `stream closed before response.completed`，触发 CLI 原生采样重试 1/5。
- **9/19 00:47:46 北京时间**：`MC202609181647371963478358268d9d6Rd7vYYsj`，重试响应开始出现 `item_* function_call`；同一用户回合随后短暂恢复 `exec`，最后才声称工具不可用。
- 请核查第一条 SSE 中 `response.output_item.added`、增量、`response.completed` 的存在与顺序，以及重试前后 `additional_tools/custom` 定义是否仍完整。客户端没有失败 SSE 原文，不能仅凭 CLI 报错判断是服务端丢事件还是客户端解析问题。
- 同期子线程有 6 条同类解析错误；其中部分响应仍产生真正的 `custom_tool_call: exec`。不要仅按此错误自动认定工具丢失。

补充对照：短/长文本 × 无图/有合成图片，各 2 次，共 8 次均正常；长输入约 9.5 万 input tokens。不是“只测了短无图请求”，但仍未复现真实长会话中途异常。

客户端做过攻击检测开/关对照，已捕获请求工具定义一致；最新短合成测试 12/12 正常，未捕获失败原始请求，因此不能单靠客户端证据确认服务端转换缺陷。报告不含密钥或完整业务转录。

## 02:01–02:09 客户端进一步验证（不是自然故障复现）

五组隔离实测合计 78 次 HTTP 请求、44 次真实命令执行均成功，包括同线程 16 轮、工具返回图片、单用户回合连续 12 次调用。另分别一次注入孤立文本 delta 和缺 `response.completed`：前者准确触发 CLI 的 `OutputTextDelta without active item`，后续仍能调用工具；后者增加一次采样后恢复，工具定义仍在。**不要仅凭解析报错就认定工具被移除。**

所有请求仍声明 `functions.exec`；首轮执行后完整定义哈希改变，逐字段差异只是 exec 说明新增 deferred-tools/MCP 类型，未移除 shell/文件能力。测试代理缓冲 SSE 后转发以校验工具白名单，不能用这些正常样本排除生产流时序或间歇性路由问题。失败原始请求/SSE 仍未捕获。

## 10:49 已捕获自然上游异常心跳（不是“无工具”的根因证明）

请求 `MC202609190249243565167998268d9d6i8TtL10B`，HTTP 200 原始 SSE 含：

```json
{"type":"response.output_text.delta","item_id":"SSE-Keep-Alive","output_index":0,"content_index":0,"delta":"","SSE-Keep-Alive":true}
```

此前没有对应的 `response.output_item.added`，CLI 随后准确记录 `OutputTextDelta without active item`。请检查心跳实现，避免将保活消息伪装成不存在的正文项增量。这次已保存真实上游正文，无本地故障注入。

但该轮及后续两轮均执行命令成功，因此它只能解释该条解析错误，不能证明它导致工具消失。真实 Chill Vibe 启动路径新增 8 轮、18 次请求、8 次命令执行均保留工具；其中两次 503 后请求体逐字不变，重试成功。真实“无工具”失败请求仍未抓获，需要继续核查前述业务请求的工具转接与渠道映射。
